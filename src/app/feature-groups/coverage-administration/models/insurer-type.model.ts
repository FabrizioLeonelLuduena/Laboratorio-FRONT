/**
 * Insurer type response
 */
export interface InsurerTypeResponseDTO {
  /** Enum name of the insurer type. */
  name: string;
  /** Human-readable description of the insurer type. */
  description: string;
}
